#pragma once

// Public API for Range is provided in the project include tree. Make this a thin wrapper
// so sources that include "Range.h" continue to work, but the canonical declaration lives
// in "include/SDK/PaymentProcessor/Settings/Range.h".

// SDK
#include <SDK/PaymentProcessor/Settings/Range.h>
