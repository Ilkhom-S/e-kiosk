/* @file Шаблон объявления фабрики плагинов. */

#pragma once

// Plugin SDK
#include <SDK/Plugins/PluginFactory.h>

/// Фабрика плагинов TemplatePlugin.
/// Отвечает за создание экземпляров плагина TemplatePlugin.
/// Наследуется от базовой PluginFactory и переопределяет методы для создания плагина.
class TemplatePluginFactory : public SDK::Plugin::PluginFactory {
    Q_OBJECT
    Q_INTERFACES(SDK::Plugin::IPluginFactory)
    Q_PLUGIN_METADATA(IID "SDK.Plugin.PluginFactory")

  public:
    /// Конструктор фабрики.
    TemplatePluginFactory();

    /// Деструктор фабрики.
    ~TemplatePluginFactory();
};

//------------------------------------------------------------------------------