; EKiosk конфигурационный файл
; Все параметры должны быть задокументированы на русском языке
; Этот шаблон используется для генерации ekiosk.ini при сборке приложения

[Общие]
; Рабочий каталог приложения
WORKING_DIRECTORY=@WORKING_DIRECTORY@

; Путь к логам
LOG_PATH=@LOG_PATH@

; Язык интерфейса (ru, tj, uz, en)
LANGUAGE=ru

; Включить тестовый режим (true/false)
TEST_MODE=false

; Описание:
; - WORKING_DIRECTORY: Папка, в которой приложение хранит временные файлы и логи
; - LOG_PATH: Путь к файлу лога
; - LANGUAGE: Язык интерфейса
; - TEST_MODE: Включает режим отладки (без ограничений киоска)
