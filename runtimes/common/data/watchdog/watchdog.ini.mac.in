[module_ekiosk]
file={WS_DIR}/ekiosk.app

[module_updater]
file={WS_DIR}/updater.app

[taboo]
; macOS-specific taboo applications to be controlled (recommended):
; Activity Monitor, Terminal, Safari, Firefox, Chrome, Opera
; For debug, only Terminal is enabled below. Uncomment and add more as needed.
applications=Terminal
check_timeout=60000
