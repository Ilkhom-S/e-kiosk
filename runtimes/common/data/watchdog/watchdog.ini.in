; WatchService (watchdog) конфигурационный файл - Generic/Common sections
; Все параметры должны быть задокументированы на русском языке
; Этот шаблон используется для генерации watchdog.ini при сборке приложения
; Platform-specific paths are merged from platform-specific templates

[Общие]
; Путь к логам
LOG_PATH=@LOG_PATH@

; Таймаут проверки
CHECK_TIMEOUT=60000

; Описание:
; - LOG_PATH: Путь к файлу лога
; - CHECK_TIMEOUT: Таймаут проверки (мс)

[taboo]
; Запрещенные приложения
applications=explorer.exe,taskmgr.exe,processhacker.exe,iexplore.exe,browser.exe,chrome.exe,firefox.exe,opera.exe

; Таймаут проверки запрещенных приложений
check_timeout=60000

; Описание:
; - applications: Список запрещенных приложений через запятую
; - check_timeout: Таймаут проверки запрещенных приложений (мс)

[Module1]
; Имя модуля
name=ekiosk

; Рабочая директория модуля
workingdirectory={WS_DIR}

; Режим запуска (auto/manual)
startmode=auto

; Автоматический запуск при старте watchdog
autostart=true

; Приоритет запуска (меньше - раньше)
priority=1

; Приоритет закрытия (меньше - раньше)
close_priority=1

; Задержка после запуска (мс)
afterstartdelay=5000

; Максимальное количество перезапусков
maxstartcount=3

; Графический интерфейс (true/false)
gui=true

; Таймаут первого пинга (мс)
firstpingtimeout=30000

; Таймаут принудительного завершения (мс)
kill_timeout=10000

; Описание параметров модуля:
; - name: Уникальное имя модуля для идентификации
; - file: Путь к исполняемому файлу (поддерживает {WS_DIR} как рабочую директорию watchdog)
; - workingdirectory: Рабочая директория для запуска модуля
; - startmode: Режим запуска - auto (автоматический) или manual (ручной)
; - autostart: Автоматический запуск модуля при старте watchdog
; - priority: Приоритет запуска (меньше число = выше приоритет)
; - close_priority: Приоритет закрытия (меньше число = выше приоритет)
; - afterstartdelay: Задержка после успешного запуска модуля (мс)
; - maxstartcount: Максимальное количество попыток перезапуска при сбое
; - gui: Наличие графического интерфейса у модуля
; - firstpingtimeout: Таймаут ожидания первого пинга от модуля (мс)
; - kill_timeout: Таймаут принудительного завершения процесса (мс)

[module_updater]
; Имя модуля
name=updater

; Рабочая директория модуля
workingdirectory={WS_DIR}

; Режим запуска (auto/manual)
startmode=auto

; Автоматический запуск при старте watchdog
autostart=false

; Приоритет запуска (меньше - раньше)
priority=3

; Приоритет закрытия (меньше - раньше)
close_priority=2

; Задержка после запуска (мс)
afterstartdelay=0

; Максимальное количество перезапусков
maxstartcount=1

; Графический интерфейс (true/false)
gui=false

; Таймаут первого пинга (мс)
firstpingtimeout=30000

; Таймаут принудительного завершения (мс)
kill_timeout=10000
