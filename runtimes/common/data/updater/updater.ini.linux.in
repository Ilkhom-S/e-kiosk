; Updater configuration template - Linux specific sections
; This file contains only Linux-specific overrides/additions
; Will be merged with the generic template

[validator]
; Required files for updater validation (Linux)
required_files=controller, ekiosk, watchdog, libbill_acceptors.so
