; Updater configuration template - Windows specific sections
; This file contains only Windows-specific overrides/additions
; Will be merged with the generic template

[validator]
; Required files for updater validation (Windows)
required_files=controller.exe, ekiosk.exe, watchdog.exe, bill_acceptors.dll
