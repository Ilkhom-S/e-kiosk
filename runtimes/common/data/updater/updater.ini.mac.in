; Updater configuration template - macOS specific sections
; This file contains only macOS-specific overrides/additions
; Will be merged with the generic template

[validator]
; Required files for updater validation (macOS)
required_files=controller.app, ekiosk.app, watchdog.app, libbill_acceptors.dylib
